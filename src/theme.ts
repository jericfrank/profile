// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#fff',
    },
  },
  typography: {
    fontFamily: '"Ancizar", "Helvetica", "Arial", sans-serif',
  },
});

export default theme;
